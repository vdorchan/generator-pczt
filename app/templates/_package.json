{
  "name": "<%= appname %>",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "<%= pageAuthor %>",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^8.1.0",<% if(includePolyfill) { %>
    "babel-plugin-transform-runtime": "^6.23.0",<% } %>
    "babel-preset-env": "^1.6.0",
    "browser-sync": "^2.18.8",
    "del": "^2.2.2",
    "gulp": "github:gulpjs/gulp#4.0",<% if(includeBabel) { %>
    "babel-core": "^6.26.0",
    "gulp-babel": "^7.0.0",<% } %><% if(includeBase64) { %> 
    "gulp-base64": "^0.1.3",<% } %>
    "gulp-cache": "0.4.6",
    "gulp-cached": "^1.1.1",
    "gulp-clean-css": "^3.5.0",
    "gulp-concat": "^2.6.1",<% if(includeFontSpider) { %>
    "gulp-font-spider": "^0.3.6",<% } %>
    "gulp-if": "^2.0.2",
    "gulp-imagemin": "latest",
    "gulp-plumber": "^1.1.0",
    "gulp-pngquant": "^1.0.10",
    "gulp-postcss": "^7.0.1",<% if(includeSass) { %> 
    "gulp-sass": "^3.1.0",<% } %>
    "gulp-sourcemaps": "^2.6.4",<% if(includeCompressJS) { %> 
    "gulp-uglify": "^3.0.0",<% } %>
    "gulp-www1": "^0.1.8",
    "gulp-zip": "^4.0.0",<% if(includeSprites) { %>
    "gulp.spritesmith": "^6.5.0",<% } if(includeRem) { %>
    "postcss-px2rem": "^0.3.0",<% } %>
    "postcss-sprite": "^2.2.1",
    "prompt": "^1.0.0",
    "pump": "^1.0.2",
    "gulp-load-plugins": "^1.5.0",
    "gulp-replace": "^0.6.1"
  }
}